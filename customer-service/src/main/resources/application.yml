spring:
  jpa:
    defer-datasource-initialization: true
  application:
    name: customers-service
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8083/eureka